{% extends 'base.html.twig' %}

{% block title %}Annonces{% endblock %}

{% block bodyshowannonce %}
<div> 

    <h1>Annonces</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ annonce.region }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ annonce.prix }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ annonce.type }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ annonce.email }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ annonce.categorie }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ annonce.duree }}</td>
            </tr>
            <tr>
                <th>Image</th>
              <td>  <img class="left"src={{ asset('uploads/imaage/'~ annonce.image) }} width="1000" height="500" alt="" /></td> 

            </tr>
            <div> 
              <table class="table">
        <tbody>
            {% if annonce.avis|length > 0 %}
            <tr>
            <th>Avis</th>
            {% for avi in annonce.avis %}
            <tr>
                
                <td>{{ avi.avis }}</td>
            </tr>
            {% endfor %}
            </tr>
        </tbody>
         {% endif %}
    </table>
    </di

            
        </tbody>
     


    </table>
    <a href="{{ path('app_avis_newclient', {'id': annonce.id}) }}" class="category-b">Avis</a>
    <a href="{{ path('app_client_controllerr_index') }}">back to list</a>
   <div>
    <a href="{{ path('app_client_controllerr_edit', {'id': annonce.id}) }}" class="btn btn-b">Modifier</a>
</div> 


    {{ include('client_controllerr/_delete_form.html.twig') }}
</div> 

{% endblock %}


